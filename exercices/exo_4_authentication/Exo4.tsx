import { Text, View } from 'react-native';

export const Exo4 = () => {
  // 🐨 Here you will import the `useIdentity` hook to access the authentication properties

  // 🐨 Now you can return `<ScreenLoader />` if `isLoading` property is true

  // 🐨 Then you will either return <PublicStack /> or <DrawerStack /> based on the `identity` property

  // 💥 Delete this code below when you are done with the above instructions
  return (
    <View>
      <Text>Exo 4</Text>
    </View>
  );
};
